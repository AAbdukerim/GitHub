package strings;

import java.util.Arrays;

public class StringManipulation {

	/**
	 * Write method that reverses the input string
	 * 
	 * @param src
	 *            input
	 * @return reverse of input
	 */
	public String reverse(String src) {

		// TO DO

		return "TODO";
	}

	/**
	 * Write the code that checks if a given input string is a palindrome
	 * 
	 * @param src
	 *            input string
	 * @return true if the input string is palindrome. False otherwise
	 */
	public boolean palindrome(String src) {
		// TO DO
		return false;
	}

	/**
	 * You have string of even length, return a string made of the middle two
	 * characters, so the string "string" returns "ri". The string length will
	 * be at least 2.
	 * 
	 * middleTwo("string") → "ri" middleTwo("code") → "od" middleTwo("Practice")
	 * → "ct"
	 * 
	 * 
	 */
	public String middleTwo(String str) {
		// TO DO
		return "";
	}

	/**
	 * write a method that accepts a string and returns another string
	 * containing only unique charters from argument
	 * 
	 * uniqueChars("aabc11234pp") -> "abc1234p" uniqueChars("hello") -> "helo"
	 * uniqueChars("java") -> "jva"
	 * 
	 * @param mixed
	 * @return
	 */

	public String uniqueChars(String mixed) {
		// TO DO
		return "";
	}

	/**
	 * Write a method that accepts a string and a char your code needs to count
	 * how many argument chars appear in argument string
	 * 
	 * countChars("hello", 'l') -> 2 countChars("java", 'j') -> 1
	 * 
	 * @param str
	 * @param ch
	 * @return
	 */

	public int countChars(String str, char ch) {

		// TO DO
		return -1;
	}

	/**
	 * Write a method that takes has two string parameters. Your method should
	 * add those string together. So we pass "Hi" and "World", it should return
	 * "HiWorld". However, if when adding, if the last character of the first
	 * parameter is same as the first character of the second parameter, remove
	 * one of the characters. So we pass "hello" and "oscar", it should return
	 * "helloscar". 
	 * addStrings("abc", "cat") → "abcat" 
	 * addStrings("dog", "cat") → "dogcat" 
	 * addStrings("abc", "") → "abc"
	 * 
	 * @param a
	 * @param b
	 * @return
	 */

	public String addStrings(String a, String b) {
		// TO DO
		return "TODO";
	}

}
